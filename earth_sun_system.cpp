#include <iostream>
#include <cmath>

void integrate_one_step(vec position, vec velocity, vec acceleration, double time_step){
    position += time_step*velocity
    velocity += time_step*acceleration
}

int main(){
    // initialise the x, v, a for the sun and the earth

    // make a one step 

    // Write the new parameters to file. 

    


    
}